<UserControl x:Class="GKYU.PresentationLogicLibrary.Transactions.TransactionSaleModelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GKYU.PresentationLogicLibrary.Transactions"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="450">
    <Grid HorizontalAlignment="Stretch">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="175"/>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition Width="25"/>
            <ColumnDefinition Width="25"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding QuantityDescription}"/>
        <TextBlock Grid.Column="1" Text="{Binding Description}"/>
        <TextBlock Grid.Column="2" Text="{Binding PriceDescription}"/>
        <TextBlock Grid.Column="3" Text="{Binding AmountDescription}"/>
        <TextBlock Grid.Column="4" Text="{Binding TaxCode}"/>
        <TextBlock Grid.Column="5" Text="{Binding UtilizationCode}"/>
    </Grid>
</UserControl>
